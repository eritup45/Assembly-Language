“README.txt” file: 文字檔，描述你程式的內容、如何編
譯程式、程式的執行環境、如何執行你的程式

First, give values to register r0,r1,r2,r3 by "mov". (ex. mov r0, #0)
Second, add value to register r0 by "add". (ex. add r0,r1,r2)

Use arm-none-eabi-gcc -g -O0 hw2.s –o hw2.exe to compile the program on ubuntu.

We can also use arm-none-eabi-insight to check our program.
